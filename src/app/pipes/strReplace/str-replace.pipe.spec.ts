import { StrReplacePipe } from './str-replace.pipe';

describe('StrReplacePipe', () => {
  it('create an instance', () => {
    const pipe = new StrReplacePipe();
    expect(pipe).toBeTruthy();
  });
});
