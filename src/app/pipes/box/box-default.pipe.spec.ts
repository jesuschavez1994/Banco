import { BoxDefaultPipe } from './box-default.pipe';

describe('BoxDefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new BoxDefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
