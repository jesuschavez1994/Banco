import { ImgFallbackPipe } from './img-fallback.pipe';

describe('ImgFallbackPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgFallbackPipe();
    expect(pipe).toBeTruthy();
  });
});
