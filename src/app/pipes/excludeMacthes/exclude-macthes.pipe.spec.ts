import { ExcludeMacthesPipe } from './exclude-macthes.pipe';

describe('ExcludeMacthesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeMacthesPipe();
    expect(pipe).toBeTruthy();
  });
});
